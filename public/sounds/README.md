# Sound effects

Drop optional sound files here (the app falls back to a synthesized beep if missing):

- `win.mp3` — played when the player wins
- `daub.mp3` — played when marking a cell
- `join.mp3` — played when joining a game

Keep them short (< 1s) and small.
